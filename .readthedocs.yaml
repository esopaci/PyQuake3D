# .readthedocs.yaml
# Read the Docs configuration file
# See https://docs.readthedocs.io/en/stable/config-file/v2.html for details

version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"   # safer than 3.13, since many libs don’t support 3.13 yet

sphinx:
  configuration: docs/source/conf.py   # path to your conf.py

python:
  install:
    - requirements: docs/requirements.txt
